import 'package:flutter/material.dart';

class Visited extends StatelessWidget {
  const Visited({super.key});
  @override
  Widget build(context) {
    return Column(children: [Text("Visited Locations")]);
  }
}
